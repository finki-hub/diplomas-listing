name: Lint Diplomas API

on:
  push:
    branches: [main, dev]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tsc:
    uses: finki-hub/.github/.github/workflows/tsc.yaml@main
    with:
      script: check:diplomas-api
  eslint:
    uses: finki-hub/.github/.github/workflows/eslint.yaml@main
    with:
      script: lint:diplomas-api
